---
- name: ELK
  hosts: all

  tasks:

  - name: Install
    include_role:
      name: install
    become: yes
    when: tasks_type == "install" | default(False)

  - name: Remove
    include_role:
      name: remove
    become: yes
    when: tasks_type == "remove" | default(False)

